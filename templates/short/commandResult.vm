#set($subject = "$device.name: resultado de comando recebido")
Resultado de comando para [$device.name] recebido: $position.getString("result") em $dateTool.format("dd/MM/YYYY HH:mm:ss", $event.eventTime, $locale, $timezone)
